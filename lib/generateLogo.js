import { Circle } from "./lib/shapes"
import { Triangle } from "./lib/shapes"
import { Square } from "./lib/shapes"

export const generateLogo = ({text, text-color, shape, shape-color}) => {
 console.log(text)
}